describe('BTItemBulkCreatePopupBtn', () => {
  test('loads module exports', () => {
    const mod = require('../../../../pages/BudgetTracker/components/BTItemBulkCreatePopupBtn');
    const exported = mod.default || mod;

    expect(exported).toBeTruthy();
  });
});
