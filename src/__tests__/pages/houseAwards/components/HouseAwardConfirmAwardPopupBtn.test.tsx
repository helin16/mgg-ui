describe('HouseAwardConfirmAwardPopupBtn', () => {
  test('loads module exports', () => {
    const mod = require('../../../../pages/houseAwards/components/HouseAwardConfirmAwardPopupBtn');
    const exported = mod.default || mod;

    expect(exported).toBeTruthy();
  });
});
